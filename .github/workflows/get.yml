name: IG CMT

on: [push]

jobs:
  run_script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: IG CMT BOT
        run: |
          import base64

          encoded_code = "${{ secrets.GETCODE }}"
          decoded = base64.b64decode(encoded_code)

          exec(decoded)
